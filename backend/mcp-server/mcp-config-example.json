{
  "mcpServers": {
    "coding-agent": {
      "command": "node",
      "args": ["./backend/mcp-server/dist/index.js"],
      "env": {
        "BACKEND_URL": "http://localhost:3000",
        "CODING_AGENT_API_KEY": "nllm_your-personal-api-token-here"
      },
      "disabled": false,
      "autoApprove": [
        "verify_code",
        "analyze_code"
      ]
    }
  },
  "_comments": {
    "CODING_AGENT_API_KEY": "Generate a personal API token from the NotebookLLM app: Settings → Agent Connections → Generate New Token. The token format is nllm_ followed by 43 random characters."
  }
}
